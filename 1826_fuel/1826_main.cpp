#include<iostream>
#include<algorithm>

using std::make_heap;

int N;
int L, P;
int main() {
	using namespace std;
	ios::sync_with_stdio(false);
	cin.tie(0);

	cin >> N;
	for (int i = 0; i < N; i++) {
		int a, b;
		cin >> a >> b;

	}



	
}